# Changelog

## Unreleased

- 事实网、备团产物和运行资料列表改为纵向多列布局，减少不同长度卡片造成的空档。
- 运行模式将推进钟移动到章节总览之后，并将交互式推进钟作为唯一入口，避免与运行资料重复。
- 归一化模型为列表项附带的列表项来源形状，同时继续拒绝未知键和深层错误形状。
- 增加可重复的玩家包构建脚本，排除本地数据、参考素材、开发夹具、测试脚本和生成缓存。

## 0.1.4a0 - 2026-09-05

### 发布定位

- 面向现实恐怖板块的预发布版本，继续支持离线优先的 PDF 备团工作流。
- 本 Release 的玩家运行包只包含工具运行所需的源码、启动器和依赖锁文件，不包含本地数据、参考素材、测试脚本或生成产物。

### 变更

- 产物规划的技术安全阈值提高为 120，不再用无依据的 50 张上限强制合并有独立桌边用途的人物或地点。
- 兼容部分模型输出的 `{value, field_sources}` 卡片字段包装，同时继续严格拒绝未知字段、未知来源和更深层错误形状。
- 防止过期的排队状态覆盖已经失败或完成的产物任务状态。
- 章节级语义段在占用过多传输窗口时会请求语义细化，不再直接把整章交给一个归并流程。
- 语义归并请求超时时会在有限深度内无损缩小候选批次，并明确区分已完成的传输窗口与失败的语义段归并。

### 已知限制

- 现实恐怖仍是当前主要支持板块，其他板块的完整工作流仍在开发。
- 所有模型产物仍需 GM 在使用前复核；模型服务的限流、超时和账号状态由服务商决定。

## 0.1.3 - 2026-09-04

### 发布定位

- 首个正式版本，正式支持现实恐怖板块的章节级备团工作流。
- 奇幻冒险与通用备团仍在开发，尚未承诺完整工作流。

### 变更

- Python 依赖迁移到 `pyproject.toml` 和 `uv.lock`。
- Windows 启动器现在会自动准备 uv、Python 3.11 和项目运行依赖，不再要求用户手动安装 Python 或执行 pip 命令。
- 保留现有脚本式回归测试；`ruff` 和 `ty` 作为维护者开发依赖加入 uv 的 `dev` 组。
- 新备团任务采用语义段与传输窗口分离；超大语义段无损拆分，窗口观察在内部归并，只有段级结果进入复核队列。
- 强化超大语义段的归并恢复与任务终态，避免传输预算把逻辑完整的地点或事件作为独立复核结果公开。
- 改进事实候选分类边界、全局计划事实闭包和模型输出形状校验，提高大型模组的生成成功率。
- 产物任务记录完整待确认问题数量，同时在界面中提供有界预览和溢出提示。
- 完成章节级真实运行回归，确认语义分段、事实网和备团产物工作流可完整运行。

### 已知限制

- 部分模型草案可能出现局部格式不够规整；完整卡片仍需 GM 复核。
- 全局产物计划当时存在 50 张技术上限，接近上限时模型可能合并次要人物。
- 所有模型产物仍需 GM 在使用前复核；模型服务的限流、超时和账号状态由服务商决定。

## 0.1.2d - 2026-09-01

### 发布定位

- 面向公开仓库整理的现实恐怖板块 Beta 版本。
- 奇幻冒险与通用备团仍在开发，尚未承诺完整工作流。

### 变更

- 根目录 README 重写为 GM 用户指南，包含安装、`start.bat` / `start.vbs`、首次备团、模型连接、隐私和排障。
- `docs/README.md` 现在只作为维护者与 Coding Agent 的索引；既有研发历史文档保留原路径。
- 新增 `VERSION` 和本发布记录，版本标识为 `0.1.2d`。
- 项目代码采用 Apache-2.0 许可证，详见 `LICENSE`。
- 新增模型提供商调研，并在用户指南中提供 OpenRouter 的低频试用路径。
- 忽略整个 `Resource/` 目录，避免本地参考资料在之后被误加入公开源码。
- 将奈面试点的旧 `scene` 卡迁移为当前现实恐怖的 `location` 卡，恢复 shadow diff 金标准回归。

### 已知限制

- 现实恐怖仍为 Beta。所有模型草案都需要 GM 在使用前复核。
- 模型的价格、免费额度和可用性由服务商决定，可能在没有 TRPG Prep 新版本时发生变化。
- 这是面向用户的 Beta 发布包，仍需要用户准备 Python 环境和模型服务配置。
