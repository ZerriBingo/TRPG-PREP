# Bug Log - 2026-08-29 (historical)

The incidents below belong to the retired incremental-card implementation. They remain for diagnosis history only; no compatibility path is maintained for them.

## Artifact supplemental generation: cross-card fact references

- **Reported workspace**: current user bookshelf project (supplemental location-card generation)
- **Upstream**: newly configured LLM upstream
- **Observed message**: `上次生成在直接生成失败：产物 时代广场的阴影 的字段 immediate_actions 引用了卡外事实: ['fact_promoted_ccbc45edcccd9466', 'fact_promoted_bcae4021b03d148d']。请点击“重试失败步骤”；已成功完成的步骤会复用。`
- **User decision**: pause development and retain the incident for later diagnosis.
- **Current classification**: artifact validation / fact-scope integrity; not yet a confirmed root cause.
- **Required future repro seam (superseded)**: supplemental artifact job enqueue -> durable job polling -> direct-generation validation -> append to existing runtime cards.
- **Do not infer yet**: this message alone does not prove whether the defect is in prompt scoping, response normalization, field-source validation, or retry-state reuse.

### Maintenance update (2026-08-29)

- **Reproduced** from the persisted direct-generation response: the cited IDs for a populated field were present in the same supplemental input batch, but the model omitted them from that card's `fact_ids` closure.
- **Fixed**: deterministic validation now expands a card closure with field-source IDs that are present in the supplied request. References outside the supplied, promoted fact set still fail closed.
- **Regression (historical)**: `scripts/test_artifact_shape_normalization.py` covered the closure repair; the supplemental workflow test has been removed with the retired path.
- **Still open**: malformed card shapes in the same upstream response (for example legacy clock fields) remain a separate model-contract issue and are not silently converted.

### Maintenance closure (2026-08-29)

- **Task isolation (historical)**: full-board and supplemental jobs once deduplicated by request kind. The current model has only full-board jobs.
- **Visible progress**: the bookshelf artifact panel renders a determinate progress bar for queued, direct, hierarchical, materialization, validation, completion, and failure states. Only the full-board enqueue -> poll -> refresh flow remains in scope.
- **Seed cleanup**: persisted `red_signal_fixture` and `naimen_pilot` rows are removed during the service lifespan by explicit fixture ID through one storage transaction. Repository example JSON remains available for tests and explicit fixture URLs; formal bookshelf lists no longer expose them.
- **Replay evidence**: the second persisted response from `artifact_job_6f20a96467524972a05a1bdf4e89d075` now validates successfully after fact-closure repair. The first malformed clock response remains intentionally fail-closed and is recoverable through the existing retry step.

### Public seam and task-history verification (2026-08-29)

- **Regression added (historical)**: the former supplemental history test is retained here as an incident note, not as an active contract.
- **Public profile contract**: workbench profile objects expose only the neutral display contract, card definitions, and runtime semantics. Source profile `id`, `name`, `version`, and source summary are not serialized to the browser; the client uses the response map key only for internal joins.
- **Empty-state contract**: `/api/domain/workbench` with no project (including `?example=`) returns an empty workbench, and `/api/domain/export` without a project returns HTTP 400 instead of selecting a fixture.
- **Real HTTP/browser check**: after restarting the service on `127.0.0.1:8000`, empty-state responses, task history, visible progress, neutral board labels, and retry controls were verified. The current user task still has one upstream HTTP 429 failure; it remains durable and retryable rather than being rewritten or discarded.

## Artifact retry kept the old upstream model

- **Reported symptom**: after changing the OpenRouter model to `minimax/minimax-m2.7:free`, clicking retry on a bookshelf artifact task still requested `z-ai/glm-5.2:free` and returned an upstream rate-limit error.
- **Root cause**: the artifact job persisted `model_id` when it was created. The bookshelf retry endpoint only changed `status` back to `queued`; execution correctly called `make_client(model_id=job.model_id, ...)`, so it faithfully reused the stale model recorded on the failed job.
- **Fix**: retry now snapshots the current application `model` and `fake` settings into the same durable job before queueing it. The preparation task scope and profile remain unchanged.
- **Step isolation**: the model identity and fake/real mode are included in each resumable step input fingerprint and provenance refs. A retry with the same model reuses successful steps; switching models starts fresh step keys and prevents a final job from silently mixing responses from two models.
- **Regression (historical)**: the former supplemental workflow test switched from a legacy model to `minimax/minimax-m2.7:free` before retry; that test was removed with the path.

## Preparation retry kept the old upstream model (2026-08-30)

- **Reported symptom**: after changing the configured OpenRouter model, retrying a failed preparation task from the bookshelf still sent requests to the model captured when the task was first created.
- **Root cause**: the preparation retry endpoint only changed window status; `start_prep_job` and the shadow-task idempotency key continued to use the stale job model.
- **Fix**: the retry endpoint now snapshots the current `model` and `fake` settings into the durable preparation job. The idempotency key includes a short hash of that snapshot, so a model switch creates a new shadow task while retaining the old attempt history.
- **Regression**: `scripts/test_prep_retry_model.py` verifies the model snapshot, isolated shadow task, and distinct idempotency keys.

## Uploaded-source deletion compatibility (2026-08-31)

- **Reported symptom**: deleting an unused uploaded PDF from the workbench returned HTTP 405 Method Not Allowed.
- **Diagnosis**: the repository route already supported DELETE, but an older running service instance did not expose that method.
- **Fix**: added `POST /api/domain/source-files/delete` as an equivalent compatibility verb and switched the workbench delete action to it. The DELETE route remains available for current clients.
- **UI cleanup**: moved the uploaded-source list into a collapsible section inside the new-prep form, removing the duplicate standalone panel while retaining refresh, deduplication, and reference protection.
- **Regression**: `scripts/test_source_files.py` now exercises the compatibility POST route and still covers deduplication and reference-protected deletion.

## Deleted artifacts resumed an old generation task (2026-08-31)

- **Reported symptom**: after deleting an old board and reviewing a fresh p1-20 task, the shelf appeared to return the old artifacts; after deleting again, the new board would not start cleanly.
- **Root cause**: the main generation action searched artifact-job history for a failed job, while the backend create path also requeued a matching failed job. A nominally new full-board request could therefore resume old stored steps.
- **Fix**: the main action and `create_artifact_job` always create a new full-board job when no active job or cards block it. Stored steps are reused only through the failed job's explicit history-row retry button.
- **Freshness guard**: local workbench assets now return `Cache-Control: no-store`, and the HTML asset version changed so an open browser does not keep the previous UI contract.
- **Related cleanup**: the parallel source-check panel, missing-location count, warning, exclusion API, and controls were removed. Artifact review and runtime remain the product surfaces.
- **Regression**: `scripts/test_artifact_workflow.py`, `scripts/test_frontend_freshness.py`, and `scripts/test_scope_location_contracts.py` cover the separated lifecycle and retired warning.

## First promotion left the bookshelf empty until hard refresh (2026-09-01)

- **Reported symptom**: after a fresh analysis, accepting candidates with
  **复核所选并送入书架**, and then clicking **书架**, the page showed no facts
  until `Ctrl+F5`.
- **Diagnosis**: the review response contained the new `workspace_id`, but a
  workbench opened without an `example` query discarded it. The shelf view
  correctly rendered its empty state because `state.exampleId` was still
  blank. Directly opening an existing workspace worked, proving persistence
  and API reads were healthy.
- **Fix**: the client now adopts the first promotion workspace ID, updates the
  current URL with `history.replaceState`, refreshes the workspace/workbench
  data after batch promotion, and applies the same path to individual review.
- **Verification**: a real browser run from `/workbench.html?view=review`
  accepted a candidate, switched to **书架**, and rendered the 78 stored facts
  without a hard refresh. See
  `docs/MAINTENANCE_2026-09-01_SHELF_REFRESH.md`.
