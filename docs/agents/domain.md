# Domain Documentation

This repository uses a single-context layout: `CONTEXT.md` at the root and architecture decisions under `docs/adr/`.

Agents must read `CONTEXT.md` before changing domain behavior. New decisions that alter workflow, persistence, or user-visible semantics should be recorded as an ADR or maintenance note.
